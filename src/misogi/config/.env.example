# ローカル開発で本番 API を叩くときにコピーしてルートの .env に。
# 例: cp config/.env.example .env

# 本番 API のベース URL（本番ビルド時に使用）
# ※ npm run dev 時は常に /api を使い Vite プロキシ経由になるため、CORS を避けられる。この値は開発時は無視される。
VITE_API_BASE=https://51bhoxkbxd.execute-api.ap-northeast-1.amazonaws.com/prod
